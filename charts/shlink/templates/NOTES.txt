Shlink has been deployed as release {{ .Release.Name }} in namespace {{ .Release.Namespace }}.

Short domain: {{ if .Values.shlink.isHttpsEnabled }}https{{ else }}http{{ end }}://{{ .Values.shlink.defaultDomain }}

1. Get the application URL:
{{- if .Values.httpRoute.enabled }}
{{- range .Values.httpRoute.hostnames }}
  https://{{ . }}
{{- end }}
{{- else }}
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "shlink.fullname" . }} 8080:8080
  Then visit http://localhost:8080
{{- end }}

2. REST API:
  Base URL: {{ if .Values.shlink.isHttpsEnabled }}https{{ else }}http{{ end }}://{{ .Values.shlink.defaultDomain }}/rest/v3
  Health:   {{ if .Values.shlink.isHttpsEnabled }}https{{ else }}http{{ end }}://{{ .Values.shlink.defaultDomain }}/rest/health

3. Useful commands:
   helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
   helm get values {{ .Release.Name }} -n {{ .Release.Namespace }}
   kubectl logs -n {{ .Release.Namespace }} deploy/{{ include "shlink.fullname" . }} -f
