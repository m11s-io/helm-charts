SearXNG has been deployed as release {{ .Release.Name }} in namespace {{ .Release.Namespace }}.

1. Get the application URL:
{{- if .Values.httpRoute.enabled }}
{{- range .Values.httpRoute.hostnames }}
  https://{{ . }}
{{- end }}
{{- else }}
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "searxng.fullname" . }} 8080:{{ .Values.service.port }}
  Then visit http://localhost:8080
{{- end }}

2. Rate limiting:
{{- if .Values.limiter.enabled }}
   The limiter is enabled, backed by {{ if .Values.valkey.enabled }}the bundled Valkey subchart{{ else }}an external Valkey{{ end }}.
   Behind a gateway or ingress, set limiter.trustedProxies to the proxy pod CIDR,
   otherwise every request appears to come from the proxy and all traffic is
   throttled together.
{{- else }}
   The limiter is disabled. To enable it you need a Valkey database:
     helm upgrade {{ .Release.Name }} m11s/searxng \
       --set limiter.enabled=true --set valkey.enabled=true
{{- end }}

3. Useful commands:
   helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
   helm get values {{ .Release.Name }} -n {{ .Release.Namespace }}
   kubectl logs -n {{ .Release.Namespace }} deploy/{{ include "searxng.fullname" . }} -f
