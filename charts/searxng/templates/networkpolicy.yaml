{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "searxng.fullname" . }}
  labels:
    {{- include "searxng.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "searxng.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        {{- if .Values.networkPolicy.gatewayNamespace }}
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.networkPolicy.gatewayNamespace }}
        {{- else }}
        - podSelector: {}
        {{- end }}
      ports:
        - port: 8080
          protocol: TCP
  egress:
    {{- if .Values.networkPolicy.dnsEgress.enabled }}
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    {{- end }}
    {{- if or .Values.valkey.enabled .Values.valkey.url .Values.valkey.existingSecret.name }}
    - to:
        - podSelector: {}
      ports:
        - port: {{ .Values.networkPolicy.valkeyPort }}
          protocol: TCP
    {{- end }}
    {{- if .Values.networkPolicy.internetEgress.enabled }}
    - to:
        - ipBlock:
            cidr: {{ .Values.networkPolicy.internetEgress.cidr }}
            {{- with .Values.networkPolicy.internetEgress.except }}
            except:
              {{- toYaml . | nindent 14 }}
            {{- end }}
    {{- end }}
{{- end }}
