comfyui {{ .Chart.AppVersion }} has been deployed.

  Service: {{ include "comfyui.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

  To test from inside the cluster:
    kubectl run -it --rm --restart=Never curl --image=curlimages/curl -- \
      curl http://{{ include "comfyui.fullname" . }}.{{ .Release.Namespace }}/system_stats

{{- if .Values.persistence.enabled }}

  Models PVC: {{ include "comfyui.modelsClaimName" . }} ({{ .Values.persistence.size }}, mounted at {{ .Values.persistence.mountPath }})
{{- else }}

  WARNING: persistence.enabled is false. Downloaded models will not survive pod restarts.
{{- end }}

{{- if .Values.httpRoute.enabled }}

  HTTPRoute is enabled.
  {{- with .Values.httpRoute.hostnames }}
  Hostnames: {{ join ", " . }}
  {{- end }}
{{- end }}
