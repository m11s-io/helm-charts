dub has been deployed as release {{ .Release.Name }} in namespace {{ .Release.Namespace }}.

App URL: {{ .Values.dub.appUrl }}

1. Get the application URL:
{{- if .Values.httpRoute.enabled }}
{{- range .Values.httpRoute.hostnames }}
  https://{{ . }}
{{- end }}
{{- else }}
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "dub.fullname" . }} 3000:3000
  Then visit http://localhost:3000
{{- end }}

2. Useful commands:
   helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
   helm get values {{ .Release.Name }} -n {{ .Release.Namespace }}
   kubectl logs -n {{ .Release.Namespace }} deploy/{{ include "dub.fullname" . }} -f
